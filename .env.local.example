# Study Cycle - Local Development Configuration
# Copy this file to .env.local and adjust values as needed
# ⚠️  IMPORTANT: This file contains sensitive information - NEVER commit to GitHub!

# Server Configuration
PORT=3000
NODE_ENV=development
SERVER_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Database Configuration (Choose one)
# SQLite (Recommended for development - no setup required)
DATABASE_URL=./database.sqlite

# PostgreSQL (If using Docker - run: docker-compose up -d)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/study_cycle

# Authentication (Required)
JWT_SECRET=dev-jwt-secret-change-in-production-123456789-abcdef-xyz-super-secret-key

# OAuth Provider Credentials (Optional - app works without them)
# GitHub OAuth - https://github.com/settings/developers
# Create OAuth App with callback URL: http://localhost:3000/api/auth/oauth/github/callback
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Google OAuth - https://console.cloud.google.com/
# Create OAuth credentials with redirect URI: http://localhost:3000/api/auth/oauth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# AI Integration (Optional - for AI content generation)
OPENAI_API_KEY=your_openai_api_key_here

# Development Notes:
# - Replace all 'your_*_here' values with actual credentials
# - For production, use strong random strings for JWT_SECRET
# - OAuth is optional - the app works perfectly without social login
# - SQLite is recommended for development (PostgreSQL for production scaling)
